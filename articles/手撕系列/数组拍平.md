
```js
let arr = [1, [2, [3, 4]]];

function myFlat(arr) {
  let res = [];
  arr.forEach((item) => {
    if (Array.isArray(item)) {
      res.push(...myFlat(item));
    } else {
      res.push(item);
    }
  })
  return res;
}

console.log(myFlat(arr)); // [ 1, 2, 3, 4 ]
```